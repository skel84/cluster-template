---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "#{ bootstrap_cloudflare.domain.replace('.', '-') }#-staging"
spec:
  secretName: "#{ bootstrap_cloudflare.domain.replace('.', '-') }#-staging-tls"
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: "#{ bootstrap_cloudflare.domain }#"
  dnsNames:
    - "#{ bootstrap_cloudflare.domain }#"
    - "*.#{ bootstrap_cloudflare.domain }#"
