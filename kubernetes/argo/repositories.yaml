---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: repositories
  namespace: argocd
spec:
  project: home-kubernetes
  source:
    repoURL: "https://github.com/ajaykumar4/home-lab-cluster.git"
    path: kubernetes/argo/repositories
    targetRevision: "main"
    directory:
      recurse: true
  destination:
    name: in-cluster
    namespace: argocd
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: argoproj.io
      kind: Application
      jsonPointers:
        - /spec/syncPolicy
