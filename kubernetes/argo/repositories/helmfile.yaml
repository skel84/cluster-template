---
helmDefaults:
  wait: true
  waitForJobs: true
  timeout: 600
  force: true

environments:
  default:
    secrets:
      - values.sops.yaml

releases:
  - name: repositories
    namespace: argocd
    chart: .
    version: 1.0.0
    secrets:
      - values.sops.yaml
