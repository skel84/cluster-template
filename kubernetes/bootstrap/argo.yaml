---
helmDefaults:
  wait: true
  waitForJobs: true
  timeout: 600
  recreatePods: true
  force: true

repositories:
  - name: jetstack
    url: https://charts.jetstack.io

apiVersions:
  - monitoring.coreos.com/v1

releases:
  - name: cert-manager
    namespace: cert-manager
    chart: jetstack/cert-manager
    version: v1.15.1
    values:
      - ../apps/cert-manager/cert-manager/helm-values.yaml
  - name: argocd
    namespace: argocd
    chart: ../apps/argocd/argocd
    version: 7.3.4
    values:
      - ../apps/argocd/argocd/values.yaml
    secrets:
      - ../apps/argocd/argocd/values.sops.yaml
    needs:
      - cert-manager/cert-manager
