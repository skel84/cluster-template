---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: cloudflared
spec:
  endpoints:
    - dnsName: "external.#{ cloudflare.domain }#"
      recordType: CNAME
      targets: ["#{ cloudflare.tunnel.id }#.cfargotunnel.com"]
